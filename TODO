Bugs
====

* velo => 0, velo => 64 does not come back as chord player.
* notify host on changes => mark proj as dirty (undo ?)
* should work without a connected Launchpad.
* missing sequencer setting on song copy



probability
===========

If left-most bit of 'ctrl' is 1 on then 'ctrl' becomes 'probability'

flags
=====

Add 'flags': type (note, chord, chord changer, chord player, pattern change)

shuffle
=======

Global parameter in 'pattern' ? How do we shuffle ?
